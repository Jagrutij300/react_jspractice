import React from 'react';  //conversion react code in js 
import ReactDOM from 'react-dom/client'; //conversion react code in js code
import App from './App.js'; //. (dot)means current folder


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
  );

